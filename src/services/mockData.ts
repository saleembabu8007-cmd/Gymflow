import {
  DEFAULT_GYM_ID,
  DEFAULT_USER_ID,
  mockGym,
  mockUser,
  mockSettings,
  mockPlans,
  mockMembers,
  mockMemberships,
  mockPayments,
  mockReminders,
} from '../data/mockData';

export const INITIAL_GYM = mockGym;
export const INITIAL_USER = mockUser;
export const INITIAL_SETTINGS = mockSettings;
export const INITIAL_PLANS = mockPlans;
export const INITIAL_MEMBERS = mockMembers;
export const INITIAL_MEMBERSHIPS = mockMemberships;
export const INITIAL_PAYMENTS = mockPayments;
export const INITIAL_REMINDERS = mockReminders;

export { DEFAULT_GYM_ID, DEFAULT_USER_ID };
